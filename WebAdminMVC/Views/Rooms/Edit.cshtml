@model WebAdmin.MVC.Models.Rooms.RoomEditVm
@{
    Layout = "_Layout";
    ViewData["Title"] = "Edit Room";
}
<div class="container">
    <div class="card pad-20">
        <div class="card-head">
            <div class="card-title">Edit Room</div>
            <a class="link" href="/Rooms/Index">Back</a>
        </div>

        <form method="post" class="form-grid">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="Id" />

            <div class="fg">
                <label class="lbl">Name</label>
                <input class="inp" asp-for="Name" />
            </div>
            <div class="fg">
                <label class="lbl">Block</label>
                <input class="inp" asp-for="Block" />
            </div>
            <div class="fg">
                <label class="lbl">Floor</label>
                <input class="inp" asp-for="Floor" />
            </div>
            <div class="fg">
                <label class="lbl">Price</label>
                <input class="inp" asp-for="Price" />
            </div>
            <div class="fg">
                <label class="lbl">Status</label>
                <select class="inp" asp-for="Status">
                    <option>Available</option>
                    <option>Occupied</option>
                    <option>Maintenance</option>
                </select>
            </div>

            <div class="form-actions">
                <a class="link" href="/Rooms/Index">Cancel</a>
                <button class="btn-gradient" type="submit">Save</button>
            </div>
        </form>
    </div>
</div>
