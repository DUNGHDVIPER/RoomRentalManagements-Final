@model BLL.DTOs.Property.BlockDto

@{
    Layout = "_Layout";
    ViewData["Title"] = "Edit Block";
}

<div class="container">

    <div class="card pad-20">

        <div class="card-head">
            <div class="card-title">Edit Block</div>
            <a class="link" asp-action="Index">Back</a>
        </div>

        <form method="post" class="form-grid">

            @Html.AntiForgeryToken()

            <input type="hidden" asp-for="BlockId" />

            <div class="fg">
                <label class="lbl">Block Name</label>
                <input class="inp" asp-for="BlockName" />
            </div>

            <div class="fg">
                <label class="lbl">Address</label>
                <input class="inp" asp-for="Address" />
            </div>

            <div class="fg">
                <label class="lbl">Note</label>
                <input class="inp" asp-for="Note" />
            </div>

            <div class="form-actions">
                <a class="link" asp-action="Index">Cancel</a>
                <button class="btn-gradient" type="submit">Save</button>
            </div>

        </form>

    </div>
</div>